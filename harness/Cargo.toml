[package]
name = "harness"
version = "0.1.0"
edition = "2021"

[features]
default = ["oracle"]
oracle = []
rust = ["dep:crabvpx"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
indicatif = "0.17"
crabvpx = { path = "../", optional = true }

[build-dependencies]
bindgen = "0.69"
